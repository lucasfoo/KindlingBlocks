module.exports = {
    // **optional** default: `[{ root: './' }]`
    // support monorepos
    projects: [
      './fe/hello-world', // Shorthand for specifying only the project root location
    ]
  }